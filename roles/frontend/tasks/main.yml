---
# Application setup tasks (nginx, node, environment)
- include_tasks: application.yml

# Deployment tasks (SSH keys, git clone, dependencies)
- include_tasks: deployment.yml

# Static build tasks
- include_tasks: static_build.yml

# SSR build tasks
- include_tasks: ssr_build.yml
